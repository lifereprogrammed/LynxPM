@model IEnumerable<LynxPMCore.Models.WorkGroup>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.WorkGroupName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.WorkGroupDrescription)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.WorkGroupName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkGroupDrescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WorkGroupID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WorkGroupID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WorkGroupID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
